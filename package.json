{
  "name": "user-management-fullstack",
  "private": true,
  "packageManager": "npm@10",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "prepare": "husky",
    "lint": "npm run -w backend lint && npm run -w frontend lint",
    "format": "npm run -w backend format && npm run -w frontend format",
    "test": "npm run -w backend test && npm run -w frontend test",
    "build": "npm run -w backend build && npm run -w frontend build",
    "dev": "npm run -w backend dev",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down -v"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.6",
    "prettier": "^3.5.3"
  },
  "lint-staged": {
    "**/*.{js,cjs,mjs,ts,tsx,vue,json,md,yml,yaml}": [
      "prettier --write"
    ]
  }
}
